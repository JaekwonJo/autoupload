# CODEX 연속작업 인수인계 문서 (2026-03-04)

## 0) 이 문서의 목적
- 사용자가 채팅창을 껐다가 다시 켜도, 이전 대화 맥락과 작업 상태를 1:1로 이어가기 위한 기록 문서.
- 다음 Codex는 **이 문서를 먼저 읽고** 작업을 시작한다.

---

## 1) 다음 Codex 시작 절차 (필수)
1. `AGENTS.md` 확인
2. `codex 설명서.md` 확인
3. 이 문서(`CODEX_연속작업_인수인계_20260304.md`) 전체 확인
4. 작업 상태 확인:
   - `git status --short`
   - `python3 -m py_compile flow/flow_auto_v2.py`
5. 사용자에게 현재 반영 상태 + 남은 작업을 짧게 보고 후 진행

---

## 2) 사용자 요청 성향 / 중요한 운영 원칙
- 한국어로 아주 쉽게 설명해야 함.
- 기존 기능이 잘 돌고 있을 때, 신규 기능은 반드시 **분리 모듈**로 넣어 기존 기능 영향 최소화.
- 버튼/모드 분리를 매우 중요하게 생각함.
- “일시정지/재개”, “기존 브라우저 유지” 같은 실사용 편의성을 선호함.
- 셀렉터 자동찾기/테스트 기능을 선호하며, 실패 시 로그 기반으로 빠른 보정을 원함.

---

## 3) 최근 대화/요청 흐름 요약 (순서대로)
1. S반복 자동화 요청:
   - `시작` 클릭 -> `에셋 검색` -> `S1` 입력+Enter -> 프롬프트 입력.
   - `S1~S#` 자동 반복.
2. 기존 프롬프트 자동화와 S반복 자동화 버튼 분리 요청.
3. 성능/끊김 이슈 문의.
4. S반복에서 `시작/에셋 검색` 단계가 누락되는 오류 로그 제공, 반복 수정.
5. 동적 UI ref 재할당 문제 제기:
   - 고정 ref 재사용 금지.
   - 텍스트 기준 재탐색 방식 필요.
6. S반복 성공.
7. 프롬프트 모드/브라우저 재사용 이슈 수정.
8. 랜덤 간격 관련 설명 요청.
9. 시작/중지 버튼 위치를 Mini HUD 아래로 이동 요청.
10. selector 자동찾기/테스트 “매번 해야 하는지” 질문.
11. X 클릭 시 종료 대신 트레이 숨김 요청 + 트레이 종료 시 확인 팝업 요청.
12. `{tag}` 의미 질문 + 중지 대신 일시정지/재개 요청.
13. 다른 PC에서도 원터치 실행/설치 요청.
14. 설치 마법사(.exe) + GitHub 최종 배포(2026-03-03 Ver.01) 요청.
15. 추가 아이디어:
   - 이미지/영상 결과물 다운로드 자동화
   - hover 기반 `...` 메뉴 -> `다운로드` -> 품질 선택
   - 이미지/영상 selector 자동찾기/테스트 분리
   - 모드A(최신 1개씩) + 모드B(전체 순차) 동시 지원
   - 이미지 4K 기본, 영상 1080p 기본
   - 파일명 커스텀 불필요, 사이트 추천 파일명 유지
   - 저장 폴더는 이미지/영상 분리
   - 중복 방지 + 재시작 이어받기

---

## 4) 이미 완료된 큰 기능 (확정)

### 4-1. Ver.01 릴리스 반영 완료
- 앱 버전 표기: `2026-03-03 Ver.01`
- 트레이 숨김/종료 확인
- 일시정지/재개 (브라우저 유지), 완전중지(브라우저 종료) 분리
- 원터치 설치+실행 배치
- 설치 마법사(Inno Setup) 스크립트
- GitHub 반영 완료:
  - commit: `bb49db9`
  - tag: `v2026.03.03-ver.01`

### 4-2. 관련 파일
- 메인: `flow/flow_auto_v2.py`
- 원터치: `0_원터치_설치+실행.bat`
- 설치판 런처: `FlowVeo_실행.bat`
- 무음 실행: `Flow_Start.vbs`, `run_silent.vbs`
- 설치 마법사:
  - `release/build_installer.bat`
  - `release/installer/FlowVeo_20260303_Ver01.iss`
  - `release/배포_가이드_20260303_Ver01.md`

---

## 5) 현재 진행 중 (아직 커밋 전) - 다운로드 자동화 모듈

> 상태: `flow/flow_auto_v2.py`에 대규모 추가 완료, 문법 검사 통과.  
> 아직 실환경 E2E 테스트/미세 보정/커밋은 미완.

### 5-1. 추가된 핵심 구조
- 다운로드 전용 config 키 추가:
  - `download_strategy` (`all`/`latest`)
  - 이미지/영상 품질
  - 이미지/영상 저장 폴더
  - 이미지/영상 각각 메뉴/다운로드/품질 selector 커스텀 값
- 다운로드 중복 방지 manifest:
  - 파일: `download_manifest.json`
  - media별 key 저장(`image`/`video`)
- Playwright context에 `accept_downloads=True` 적용

### 5-2. UI 추가 (분리 모듈)
- 설정 영역:
  - 다운로드 모드(A/B), 품질, 폴더 설정
  - 이미지 selector 자동찾기/테스트 버튼
  - 영상 selector 자동찾기/테스트 버튼
- 실행 컨트롤:
  - `▶ 이미지 다운로드 자동화 시작`
  - `▶ 영상 다운로드 자동화 시작`
- 기존 프롬프트/S반복 버튼은 유지

### 5-3. 런타임 분기
- `current_run_mode`에 `download_image`, `download_video` 추가
- `on_start()`와 `_run_task()`에서 다운로드 모드 분기 처리
- 다운로드 모드에서는 프롬프트 입력/제출 루틴을 타지 않음

### 5-4. 다운로드 엔진 구현 요약
- 카드 탐색:
  - 페이지에서 `img` 또는 `video` 카드 후보 스캔
  - 위치/텍스트/src 기반 해시 key 생성
- 메뉴 오픈:
  - 카드 hover -> `...` 버튼 탐색/클릭
  - `다운로드` 메뉴 hover
  - 품질 메뉴(`4K/2K/1K` 또는 `1080p/720p/270p`) 선택
- 저장:
  - Playwright `expect_download()` 사용
  - suggested filename 그대로 저장(사용자 요청 반영)
  - 충돌 시 `(2)`, `(3)` suffix 부여
- 중복/이어받기:
  - manifest key 존재 시 skip
  - 중지 후 재시작 시 manifest 기준 이어서 수행

---

## 6) 현재 워크트리 상태 (중요)
- `git status --short` 기준:
  - `M flow/flow_auto_v2.py`
  - `M flow/flow_config.json`

주의:
- `flow/flow_config.json`은 사용자 로컬 실행값이므로 보통 커밋 제외 권장.
- 다운로드 모듈은 아직 사용자 실환경 검증 전이므로, 테스트 후 커밋 권장.

---

## 7) 다음 작업자가 해야 할 우선순위
1. 사용자 실환경에서 selector 테스트 먼저:
   - 이미지 auto-detect/test
   - 영상 auto-detect/test
2. 다운로드 실동작 테스트:
   - 모드A(`latest`) 3회
   - 모드B(`all`) 스크롤 이어받기 포함 10개 이상
3. 실패 로그 수집 후 미세 보정:
   - `...` 버튼 hover 타이밍
   - 다운로드 메뉴 hover 안정성
   - 품질 submenu 탐색 범위
4. 검증 통과 시 커밋/푸시:
   - `flow/flow_config.json` 제외 커밋 권장

---

## 8) 테스트 체크리스트 (현장용)
- 공통
  - [ ] `python3 -m py_compile flow/flow_auto_v2.py` 통과
  - [ ] 앱 실행 시 기존 프롬프트/S반복 정상
  - [ ] 일시정지/재개/완전중지 정상
- 이미지 다운로드
  - [ ] selector 자동찾기 성공
  - [ ] selector 테스트 통과
  - [ ] 4K 다운로드 성공
  - [ ] 1K로 바꿔도 성공
  - [ ] 이미지 폴더 분리 저장 확인
- 영상 다운로드
  - [ ] selector 자동찾기 성공
  - [ ] selector 테스트 통과
  - [ ] 1080p 다운로드 성공
  - [ ] 720p로 바꿔도 성공
  - [ ] 영상 폴더 분리 저장 확인
- 모드
  - [ ] latest(모드A) 동작 확인
  - [ ] all(모드B) 순차 다운로드 + 스크롤 탐색 확인
- 중복/이어받기
  - [ ] 같은 카드 재실행 시 skip
  - [ ] 중지 후 재시작 이어받기 확인

---

## 9) 사용자에게 다음에 이렇게 안내하면 됨
- “우선 `CODEX_연속작업_인수인계_20260304.md` 문서를 열어보세요.”
- “그다음 지금 상태에서 다운로드 모듈 실테스트부터 이어서 진행하겠습니다.”

---

## 10) 참고 파일
- `AGENTS.md`
- `codex 설명서.md`
- `BUILDLOG.md`
- `PROJECT_STATE.md` (현재 오래된 내용 일부 포함)
- `flow/flow_auto_v2.py`

